from sqlalchemy import Column, Integer, String, Text, Boolean, DateTime
from datetime import datetime
from app.models.database import Base


class QARuleSet(Base):
    __tablename__ = "qa_rulesets"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(200), nullable=False)
    description = Column(Text, nullable=True)
    service_type = Column(String(100), nullable=True)   # ERP, 쇼핑몰, 모바일앱 등

    tree_rules = Column(Text, nullable=True)   # 메뉴트리 추출 추가 지침 (관점 가이드, 보조)
    tc_rules = Column(Text, nullable=True)     # TC 생성 추가 지침
    flow_rules = Column(Text, nullable=True)   # 흐름 트리 구조 문법 규칙 (PR/C/D 배치 — 필수 준수)

    is_default = Column(Boolean, default=False)
    is_system = Column(Boolean, default=False)  # 시스템 제공 룰셋(삭제 불가)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)


DEFAULT_TREE_RULES = """[관점 가이드 — 기획서에 기술된 요소를 빠뜨리지 않기 위한 체크 관점]
아래 항목은 트리 추출 시 "기획서에 이런 요소가 명시되어 있는지" 살펴볼 관점입니다. 기획서에 명시되지 않은 요소를 추측해 노드로 추가하지 마세요.

[테스트 대상이 되는 UI 요소 관점]
기획서가 다음 요소를 명시하고 있다면 리프 노드로 포함하세요:
- 입력란(input): 텍스트박스, 검색창, 날짜입력, 숫자입력
- 버튼(button): 조회·저장·삭제·취소·팝업 트리거 버튼
- 표시영역(display): 목록, 상세정보, 라벨, 타이틀, 카운트
- 드롭다운(dropdown), 체크박스/라디오, 탭, 모달/팝업

[상태 분기 처리 — 흐름 트리에서는 PR(사전조건/상태) 노드로 분기]
- 권한(읽기만/사용안함/읽기쓰기), 코드·계약 상태, 시스템 설정, 데이터 존재 여부 같은 상태 차원은 흐름 트리에서 **PR 노드로 분기**해 표현합니다(상태 값마다 별도 PR, 결과가 같아도 분리).
- TC는 이 PR 경로를 선형화할 때 자동으로 사전조건이 됩니다. 따라서 "상태를 트리 노드로 두지 말고 TC 사전조건으로만" 같은 (구 구조적 트리용) 지침은 흐름 트리에 적용하지 않습니다.

[key_points 작성 — 기획서 근거가 있는 항목만]
- 기획서·매뉴얼·결함이력에 근거가 있는 검증 포인트만 적습니다.
- 기획서에 없는 일반적 제약(길이/허용 문자/필수 여부 등)을 추정해 적지 마세요. 기획서가 명시한 경우에만.
- 입력란: 기획서가 명시한 허용 문자/길이/필수 여부/플레이스홀더 문구
- 버튼: 기획서가 명시한 클릭 후 결과·팝업·툴팁 문구
- 표시영역: 기획서가 명시한 필수 출력 항목"""


DEFAULT_TC_RULES = """[관점 가이드 — 각 UI 요소에서 무엇을 살펴볼지의 관점 모음]
아래 항목은 "기획서가 해당 제약·동작·문구를 명시하고 있는지" 살펴볼 관점입니다.
**기획서·매뉴얼·결함이력에 근거가 없는 케이스는 만들지 마세요** ("빠짐없이" 채우려 하지 말 것).

[입력란(input) — 기획서에 명시된 경우에만 TC 생성]
- 플레이스홀더 문구: 기획서가 명시한 placeholder 텍스트가 있으면 그 문구 그대로 검증 TC
- 유효한 값 입력: 정상 처리 흐름 (기획서가 "유효 값" 예시를 주면 그것을 사용)
- 입력 유형 제한: 기획서가 "숫자만 허용", "한글만 허용" 등을 명시한 경우에만 위반 케이스 TC
- 길이/크기 제한: 기획서가 최대 길이를 명시한 경우에만 경계값/초과 TC. **명시되지 않은 길이 경계값 TC는 만들지 마세요.**
- 필수/공백: 기획서가 필수 여부를 명시한 경우에만 미입력 TC
- 정보 일치/불일치: 검증 API·룰을 기획서가 명시한 경우, 일치/불일치 사전조건별 TC (사람 작성 TC의 일반적 패턴)

[버튼(button) — 기획서에 명시된 동작에만 TC 생성]
- 화면 표시 / 마우스 호버 시 툴팁: 기획서가 툴팁 문구를 명시했으면 그 문구 그대로
- 클릭 정상 동작: 기획서가 명시한 결과
- 팝업 트리거: 열기 / 확인 / 취소 / 닫기를 각각 별도 TC. 단, 기획서가 그 분기를 다룬 경우에만.
- 활성/비활성 조건: 기획서가 명시한 조건별로 TC

[표시영역(display) — 기획서가 명시한 항목만]
- 필수 출력 항목: 기획서가 명시한 항목 목록을 그대로 검증
- 조건별 표시/숨김: 기획서가 분기 조건(권한·설정·데이터)을 명시한 경우에만
- 빈 데이터 처리: 기획서가 "데이터 없을 때 안내 문구" 등을 명시한 경우에만

[드롭다운/목록(dropdown/list)]
- 기획서가 항목 목록·선택 동작을 명시한 경우에만 해당 동작 검증 TC

[상태 분기 — 사전조건으로 표현 권장]
- 권한 (읽기만/사용안함/읽기쓰기 등), 외부 코드 상태 (유효/미계약/해지 등), 이력 (미신청/기신청/해지내역 등), 정보 일치/불일치 등 상태 차원은 동일 액션이라도 사전조건을 달리해 별도 TC로 분리.
- 한 TC = 하나의 (상태 조합 × 사용자 액션 × 기대결과).

[금지 — 추측성 케이스]
- 기획서에 명시되지 않은 일반적 길이/특수문자/오탈자/글머리 기호 검증 금지
- 인프라 추측(세션 만료/네트워크/F5/브라우저 호환성) 금지 — 기획서가 명시한 경우에만
- 팝업 크기 변경/최대화/최소화/반응형 같은 UI 일반론 금지
- "정확히 표시되는지 확인", "명확하게 구분되는지" 같은 추상·메타 검증 금지

[기대결과 작성 원칙]
- 기획서의 실제 문구·라벨·메시지를 그대로 인용 (예: "신청가능 정보와 일치하지 않아요." 토스트 팝업이 3초 후 페이드 아웃)
- 모르면 `<기획서 인용 필요>` 라고 적고 추상화하지 말 것"""


DEFAULT_FLOW_RULES = """[흐름 트리 정밀도 규칙 — 사람 수준으로 빠짐없이 잘게]

1. 표시 요소는 하나하나 개별 D 노드로 분해하라. 절대 뭉치지 마라.
   - 팝업/화면 하나에 타이틀·서브카피·본문·각 버튼·썸네일·안내문구·장점 항목이 있으면 전부 개별 D.
     목록은 항목마다 D(장점 4개 → 장점1·2·3·4).
   - **팝업·바텀시트·페이지 자체를 "D: 팝업 노출", "D: 바텀시트 출력", "D: 페이지 이동"처럼
     컨테이너 한 줄로 표현하지 마라.** 그 안에 실제로 보이는 타이틀·버튼·텍스트·입력란 등을
     각각 개별 D로 분해해서 나열하는 것이 올바른 표현이다.
   - "기타 UI 요소 출력"처럼 뭉뚱그린 표현 금지. 분해한 개별 D들은 화면에 실제 출력되는 순서대로
     (위→아래, 좌→우 등 실제 배치 순서) 나열하라. 임의 순서 금지.

2. 화면의 모든 형제 UI 요소를 포함하라 — 신규 기능만 보지 마라. 같은 화면의 기존 버튼·필터·영역
   (조회·인쇄·새로고침 버튼·조회 필터 등)도 D로. 화면 진입 시 기본 표시 요소를 먼저 D로 나열한 뒤 흐름 전개.

3. 상태 차원의 각 값마다 반드시 별도 PR 분기. 결과가 같아도 합치지 마라.
   (권한 읽기만/사용안함/읽기쓰기, 코드 정상/미계약/해지, 설치/미설치 등 각각 PR)

4. 기획서의 모든 화면·페이지를 빠짐없이 다뤄라. 진입점이 여러 개면 PR 루트도 그만큼.

5. 흐름은 종료 지점까지 끝까지 전개. 액션(C/T)과 결과(D)는 별도 노드로 분리:
   액션(C/T) → 결과(D) → 그 안의 또 다른 액션 → 결과 … 깊게.

6. D 노드 content엔 기획서 실제 문구를 그대로 인용(요약 금지). 추상 표현 금지.
   기획서에 없는 건 만들지 마라.
   - **D 노드는 사용자 화면에 실제로 표시되는 텍스트·이미지·버튼·레이블만 담는다.**
     시스템 내부 동작(알림톡 발송, API 호출, 데이터 처리, 검증 로직 등)은 화면에 보이지 않으므로
     D 노드가 될 수 없다. 예: "D: 신청 완료 알림톡 발송"은 잘못된 표현 — 알림톡 발송 자체는
     시스템 처리이며, 화면에 피드백이 표시된다면 그 피드백 문구(예: 완료 안내 메시지)를 D로 적어라.

7. 각 노드에 spec_page(출처 페이지 번호; 모르면 "")를 넣어라. 루트 PR content엔 menu_path(메뉴 경로)와
   그 경로에 들어가기 직전 상태를 쓰고, menu_path도 별도 필드로 넣어라.

8. 분량을 아끼지 마라. 빠짐없이 전개가 요약보다 훨씬 중요하다.

9. T(입력 행위) 노드는 예외 없이 자식 D를 가져야 한다 — 입력 필드가 있으면 D(필드 출력)만 만들고
   끝내지 말고 T(입력 행위) 노드도 반드시 별도로 만들어라(필드마다 입력 행위 자체가 누락되는 경우가 잦다).
   기획서가 입력 결과를 따로 묘사하지 않아도, 그 D content는 사람 골든 기준의 표준 문구
   "입력한 텍스트 반영"을 써라(규칙6의 원문 인용 원칙의 예외). 입력 필드가 여러 개 이어지는 경우에도
   각 T마다 빠짐없이 이 D를 붙이고, 모든 필드가 채워진 뒤의 통합 결과(예: 버튼 활성화)는
   그 다음 별도 PR/D로 추가하라.

10. 화면/메뉴 진입 직후 첫 번째 항목(사전조건 PR 바로 다음 자식)은 반드시 C(또는 T)여야 한다 —
    V나 D로 시작하지 마라. 화면에 진입했다는 것 자체가 사용자 행동(클릭 등)의 결과이므로,
    그 행동을 명시하는 C/T가 항상 흐름의 첫 단추다.

[흐름 트리 구조 문법 — PR/C/D/T/H/V 노드를 어떻게 배치할지의 필수 규칙]
(출처: QA 0624 회신 분석, docs/QA_flowtree_tagging_agreement.md §G)

1. PR은 동작(C/T/V 등) 직전에만 둔다. 동작과 동작 사이에 PR을 끼우지 마라.
   **PR의 직속 자식이 또 PR일 수 없다(PR→PR 연속 금지).** 여러 조건이 함께 하나의 결과를 게이트하면
   — 즉 조건들이 AND로 묶이면(모두 충족돼야 그 결과가 나오면) — 별도 PR 노드로 쪼개지 말고 그
   시나리오의 루트 PR **하나의 content에 조건을 전부 모아서** 써라(예: "1차 오픈 대상 단지이며 7일간
   보지 않기가 비활성화된 상태"). 반면 조건 값에 따라 완전히 다른 결과로 갈리는 **진짜 분기(OR)**는
   트리 깊은 곳에 PR을 중첩시키지 말고 그 값마다 별도의 루트 PR(별도 경로)로 분리하라.

2. (절대규칙) 트리의 사전조건(루트) PR을 제외한, 트리 안의 모든 PR은 항상
   depth = (그 PR이 게이트하는 다음 노드의 depth) − 1 이어야 한다 — 즉 PR은 자신이 게이트하는
   동작/결과의 바로 한 칸 앞(전 열)에 위치해야 한다. PR과 그 게이트 대상 사이에 다른 노드를 끼워서
   두 칸 이상 벌어지게 하지 마라(= PR의 직속 자식이 바로 그 게이트 대상이어야 한다). 사전조건 PR만
   예외로, 다음 노드와의 depth 차이가 1을 넘을 수 있다(사전조건 자체는 게이트 대상이 따로 없으므로).

3. 상태 전이가 사용자 행동(클릭/선택/입력)으로 일어난 것이면 그 행동을 반드시 C/T 노드로 명시하고,
   PR은 그 행동 이전 상태만 기술하라. PR content에 이미 "~화면에 진입된 상태"처럼 행동의 결과를
   담아버리고 그 직후 행동 노드 없이 D가 나오면 안 된다 — 화면/메뉴/팝업 전환·리스트 항목 선택은
   예외 없이 사용자 행동이 개입하므로 PR→D 직결 금지.
   - 사용자가 여러 필드에 입력을 누적한 결과로 버튼 활성화 등 UI 상태가 바뀌는 경우는
     "PR(입력이 모두 채워진 상태) → V(그 상태 확인) → D(그에 따른 UI 변화)"로 표현하라(PR→D 직결 금지, V가 다리 역할).
   - 반면 API 응답·시스템 자동 처리처럼 사용자 행동이 전혀 개입하지 않는 시스템 상태 분기는
     PR→D 직결을 허용한다(예: API 응답값에 따른 분기).
   - **PR→V 직결도 금지.** "팝업이 노출된 상태 PR → V(팝업 UI 노출 확인)", "페이지에 진입된 상태
     PR → V(페이지 UI 노출 확인)"처럼, PR content가 이미 도착한 결과(노출됨·진입됨)를 담고
     그 직후 V가 단순 확인 노드로만 나오는 패턴은 PR→D 직결과 동일한 위반이다. V도 D와 같이
     그 앞에 반드시 사용자 행동(C/T)이 있어야 하며, PR content는 그 행동 이전 상태만 담아야 한다.
   - **루트 PR(사전조건)의 메뉴 경로 표기에 가장 자주 발생하는 실수: "사용자가 A > B > C 메뉴에
     진입한/접속한 상태"처럼 이미 그 메뉴에 도착한 것으로 적지 마라.** 그 메뉴 경로(A > B > C)는
     클릭으로 들어가는 것이므로, PR content는 "A > B > C 메뉴에 진입하기 직전 상태"(또는
     "B 화면에 있는 상태"처럼 그 클릭 이전 위치)까지만 적고, 곧바로 다음 자식 노드로
     "C(예: 'C 메뉴 클릭' 또는 '미납대장 메뉴 클릭')"를 만들어 그 메뉴 진입 행위 자체를 명시하라.
     PR 다음에 그 메뉴 클릭 C 없이 바로 D나 다른 PR이 나오면 이 규칙 위반이다.

4. 모든 동작(C/T/H/V 등) 노드는 자식으로 최소 1개의 D/V 결과를 가져야 한다(결과 없는 동작 금지).
   **PR도 마찬가지로 반드시 자식(그 PR이 게이트하는 동작 또는 결과)을 가져야 한다 — PR만 덩그러니
   리프로 남아있으면 안 된다(고아 PR 금지).** 모든 D/V는 직전에 동작이 있어야 한다(규칙3의 예외 제외,
   원인 없는 결과 금지).
   - 하나의 동작(예: 제출 버튼 클릭, 화면/팝업 진입)에서 갈라지는 여러 결과·분기는 — 검증 성공/실패
     유형별 분기뿐 아니라, **같은 화면/팝업 안에 함께 있는 서로 다른 클릭 가능 요소(닫기 버튼, 체크박스,
     권한별 PR 분기 등)도 마찬가지로** — 전부 그 동작의 직속 형제 자식으로 묶어라. 분기 중 일부를 다른
     동작 뒤나 더 깊은 위치(예: D 체인의 끝)에 떼어놓지 마라 — 그 동작과의 연결이 끊겨 "동작 없는
     결과"처럼 보이게 된다.

5. 한 동작의 결과가 순차적으로 여러 개 이어지면(분기가 아니라 단순 연속 결과) 새 경로(새 PR이나
   동작 재시작)를 만들지 말고 같은 경로 안에서 D의 자식으로 계속 이어 붙여라(D→D 체인).
   새 경로(분기)는 실제로 다른 선택·다른 상태가 생길 때만 만들어라.
   - 주의: D 체인은 "그 화면/팝업에 보이는 정적 표시 요소"만 잇는다. 그 화면/팝업 안의 **클릭 가능한
     요소(버튼/체크박스/조건분기)는 D 체인의 끝에 자식으로 잇지 말고, 그 화면/팝업을 띄운 동작(C)의
     형제 자식으로 추가**하라 — D 체인의 머리와 그 클릭 요소들이 같은 부모를 공유해야 한다.
     예: "버튼 클릭(C) → 팝업노출(D) → 타이틀(D) → 서브카피(D)"까지는 체이닝하고, 그 팝업의
     "닫기 버튼(C)", "체크박스(C)", "권한별 분기(PR)"는 체인 끝(서브카피)의 자식이 아니라
     "버튼 클릭(C)"의 형제 자식으로 추가한다.

6. 한 노드에는 단일 동작만 적어라. "체크박스 클릭 후 버튼 클릭"처럼 복합 행동을 하나로 합치지 말고
   동작-결과 쌍을 둘로 분리하라.

7. 한 메시지/팝업 안의 여러 줄 텍스트는 한 D content에 `-`로 묶어서 써라(줄마다 D를 쪼개지 마라).
   단, 장점1·2·3·4처럼 서로 독립된 목록 항목은 여전히 항목마다 개별 D로 분해하라
   (규칙과 모순 아님 — "한 메시지의 줄바꿈"과 "독립 항목"을 구분).

8. 화면/팝업의 공통 UI 요소(타이틀·단지명·닫기/확인 버튼·체크박스·플레이스홀더 등)는
   신규 기능과 무관해도 빠짐없이 D로 포함하라.

9. 기획서의 "게재 위치", "게재 대상", "게재 일정" 같은 운영 메타 정보(배포/게재 계획 텍스트)는
   QA 검증 대상이 아니므로 D 노드로 만들지 마라 — 트리에 포함 금지.
   이 외에도 이미 트리 안에 존재하는 것과 같은 의미의 PR-결과 조합을 다른 위치에 또 만들지 마라(중복 노드 금지).

10. **같은 화면 진입점(동일한 사전조건 + 동일한 액션 문구)에 대해 새 루트 트리를 또 만들지 마라.**
    트리를 완성하기 전에 모든 최상위 PR의 content와 그 자식 C/T의 content를 서로 비교해서, 사전조건과
    액션 문구가 (표현이 달라도 의미상) 같은 루트가 2개 이상이면 하나로 합쳐라. 이미 그 사전조건+액션
    조합의 루트가 트리에 있다면, 새로 발견한 분기·기능은 새 루트로 떼어내지 말고 **그 루트의 자식(형제
    분기)으로 추가**하라. 한 화면/메뉴 진입 시 동시에 결정되는 여러 독립적 조건(예: "팝업 노출 여부"와
    "버튼 항상 표시 여부"가 같은 진입 시점에 함께 평가되는 경우)은 모두 같은 부모(그 진입 액션) 아래
    형제 분기로 묶어라. **사전조건 PR의 문구가 "~메뉴 진입 이전"인지 "~메뉴에 진입된 상태"인지처럼
    표현만 다를 뿐, 가리키는 화면 진입점이 같다면 이 역시 같은 루트로 합쳐야 할 신호다.**
    - **중간 화면 상태(팝업이 열린 상태, 바텀시트가 노출된 상태, 페이지에 진입된 상태 등)를
      새 루트 PR로 시작하지 마라.** 이런 상태들은 앞선 C 노드(버튼 클릭, QR 스캔 등)의
      결과여야 한다 — 해당 C 노드의 자식으로 D·PR 분기를 이어야지, 별도의 최상위 루트 PR로
      시작하면 이전 C와의 흐름이 단절된다.

11. 트리 최상위 PR(루트)들은 기획서에 등장하는 페이지 순서대로 배치하라.

12. V는 사용자 동작이 없는 시스템 자동 확인에만 써라. 사용자가 실제로 클릭·탭하는 동작이면
    V 대신 C로 적어라.

13. 상태 조건의 여집합(반대 경우)은 **기획서가 두 경우를 모두 명시했을 때만** PR 분기로 포함하라.
    기획서가 "A인 경우 ~한다"와 "A가 아닌 경우 ~한다"를 둘 다 문장으로 적었다면 두 PR을 형제로 만들어라.
    반면 기획서가 "A인 경우"만 적고 "A가 아닌 경우"에 대해서는 아무 언급이 없다면, **그 반대 경우를
    논리적으로 추론해서 PR 분기로 보강하지 마라** — 반대 경우의 실제 동작이 기획서에 없는 한 추측이며,
    "팝업/버튼이 안 보일 것"처럼 그럴듯해 보여도 실제로는 다른 동작이 있을 수 있다. 이런 단일 조건은
    PR 분기로 쪼개지 말고 그 PR 하나의 content에 조건을 명시한 채로 단일 경로로 둔다.
    (QA팀 회신: 두 경우가 기획서에 다 있으면 분기로 만드는 게 맞고, 한 가지만 있으면 반대 케이스를
    만들지 않는다.)

[★ 자주 틀리는 패턴 — 잘못된 예 vs 올바른 예]

▶ 패턴A: 팝업·바텀시트를 컨테이너 D 한 줄로 표현 (가장 자주 위반)

  ❌ 잘못된 예:
  {"type":"C","content":"버튼 클릭","children":[
    {"type":"D","content":"세대주 확인 팝업 출력","children":[
      {"type":"C","content":"아니에요 클릭","children":[...]},
      {"type":"C","content":"네, 세대주예요 클릭","children":[...]}
    ]}
  ]}

  ✅ 올바른 예: (팝업 내부 요소를 개별 D로 분해 + C들은 부모 C의 형제로)
  {"type":"C","content":"버튼 클릭","children":[
    {"type":"D","content":"간편전자고지서는 세대주만 신청할 수 있어요. 현재 세대주이신가요? (팝업 질문)","children":[]},
    {"type":"D","content":"아니에요 버튼","children":[]},
    {"type":"D","content":"네, 세대주예요 버튼","children":[]},
    {"type":"C","content":"아니에요 클릭","children":[...]},
    {"type":"C","content":"네, 세대주예요 클릭","children":[...]}
  ]}

▶ 패턴B: PR content가 이미 도착한 결과를 담음 (진입된/노출된 상태)

  ❌ 잘못된 예:
  {"type":"PR","content":"XpERP > 수납 > 미납조회 화면에 진입된 상태","children":[
    {"type":"C","content":"미납대장 메뉴 클릭","children":[...]}
  ]}

  ✅ 올바른 예: (PR은 행동 이전 상태만, 행동은 C로)
  {"type":"PR","content":"XpERP > 수납 > 미납조회 화면에 진입하기 직전 상태","children":[
    {"type":"C","content":"미납대장 메뉴 클릭","children":[...]}
  ]}

▶ 패턴C: 클릭 버튼이 D 노드의 자식으로 연결됨

  ❌ 잘못된 예:
  {"type":"D","content":"알럿: 시스템 오류입니다.","children":[
    {"type":"C","content":"확인 버튼 클릭","children":[...]}
  ]}

  ✅ 올바른 예: (확인 버튼 C는 PR/C 부모의 형제로, D는 내부 요소로 분해)
  {"type":"PR","content":"...","children":[
    {"type":"D","content":"일시적인 시스템 오류입니다. 잠시 후 다시 시도해 주세요. (알럿 메시지)","children":[]},
    {"type":"D","content":"확인 버튼","children":[]},
    {"type":"C","content":"확인 버튼 클릭","children":[...]}
  ]}"""
