# 흐름 트리 태깅 관례 — QA 합의서

> ⚠️ **이 문서는 프로그램이 읽지 않는 과거 합의 기록(변경 이력)입니다.** 지금 실제로 적용 중인 규칙은
> 항상 웹의 **[QA 룰셋 관리]** 화면(`QARuleSet.flow_rules`/`tree_rules`/`tc_rules`)에서 확인하세요.
> 이 문서 내용과 실제 룰셋이 다를 수 있습니다 — 여기는 "왜 이 규칙이 생겼는지"의 배경 설명용입니다.
> 새 QA 피드백을 반영할 때는 **룰셋 화면에서 직접 수정**하고, 이 문서는 그 변경의 배경을 남기고 싶을 때만 갱신하세요.

> ✅ **상태: 전 12개 항목 OK 합의 완료 (2026-06-22).** 사람(골든) 기준 디폴트가 수정 없이 그대로 채택됨 → FLOW_PROMPT·설계서 §4가 이미 합의안과 일치. 구현 2단계(Excel)·3단계(TC 선형화) 착수.
>
> 목적: 흐름 트리(Flow Tree) 자동 생성의 라벨링/입도/범위 관례를 QA팀과 확정한다.
> 사용법: **디폴트는 이미 사람(골든) 기준으로 설정**돼 있다. QA는 각 항목에 `OK` 또는 수정안만 적으면 그대로 구현 스펙이 된다.
> 비교 자료: 우리 PoC `docs/PoC_FlowTree_doc82_v3.xlsx` ↔ 사람 골든 `docs/human_Aegis_..._복사본.xlsx` (같은 화면 나란히 비교 권장)
> 관련: [flow_tree_schema_design.md](flow_tree_schema_design.md) §4

---

## A. 타입 코드 체계

| # | 항목 | 디폴트 (사람 기준) | QA 확정 (OK/수정) |
|---|------|-----------------|------------------|
| 1 | 사용 코드 셋 | PR, C, T, H, D, V, **DC, RC, DD** (사람 범례 전체) | OK |

---

## B. 라벨링 관례 (PoC에서 가장 많이 갈린 부분)

| # | 항목 | 우리(과거) | **디폴트 (사람 기준)** | QA 확정 |
|---|------|-----------|----------------------|---------|
| 2 | 입력 필드 | "동 입력 필드 출력" = D | **입력 행위 = T** (예: "동 입력"). 입력란/플레이스홀더 출력은 D | OK |
| 3 | 검증(V) 범위 | 관찰성 액션을 V로 남발 | **수동 확인 검증만 V** (PDF 내용 확인 등). 일반 화면 출력은 D | OK |
| 4 | 클릭→결과 | 한 셀에 섞이기도 | **액션(C/T)과 결과(D)를 별도 노드로 분리** | OK |

---

## C. 분해 입도 (얼마나 잘게)

| # | 항목 | **디폴트 (사람 기준)** | QA 확정 |
|---|------|----------------------|---------|
| 5 | 표시 요소 | 노출 요소 **하나당 D 하나**로 최대 분해 (헤드카피·QR코드·단지명·신청방법 각각). "기타 UI 요소" 같은 뭉뚱그린 표현 금지, **화면에 실제 출력되는 순서대로** 나열 (2026-06-24 §G-1 보강, 6행 근거) | OK |
| 6 | 목록 항목 | 목록은 **항목마다 D** (장점 4개 → 장점1·2·3·4) | OK |

---

## D. 범위 (무엇까지 포함)

| # | 항목 | **디폴트 (사람 기준)** | QA 확정 |
|---|------|----------------------|---------|
| 7 | 기존 화면 요소 | 이번 기능뿐 아니라 **같은 화면의 기존 버튼·필터(조회·인쇄·새로고침·필터)도 포함** | OK |
| 8 | 상태(PR) 분리 | 상태 값마다 **별도 PR. 결과가 같아도 합치지 않음** (읽기만/사용안함 각각) | OK |

---

## E. 표현/구조

| # | 항목 | **디폴트 (사람 기준)** | QA 확정 |
|---|------|----------------------|---------|
| 9 | 메뉴 경로 위치 | 루트 PR 셀에 **"경로 + 진입 상태" 함께** (예: "XpERP > 수납 > 미납조회 > 미납대장 진입된 상태") | OK — **단, G-15(화면전환 명시) 우선.** 메뉴 진입 자체가 클릭으로 일어나면 "이미 진입된 상태"로 합치지 말고 그 클릭을 C로 분리하고 PR은 클릭 이전 상태만 적는다(2026-06-24 재추출에서 이 항목이 G-15와 충돌해 누락되는 사례 확인, FLOW_PROMPT 규칙7 수정) |
| 10 | 문구 인용 | D content에 기획서 **원문 그대로 인용(요약 금지)** | OK |

---

## F. 전략 (가장 중요)

| # | 항목 | **디폴트 (사람 기준)** | QA 확정 |
|---|------|----------------------|---------|
| 11 | 산출물 형태 | **흐름 트리가 마스터**, TC 목록은 트리를 선형화한 파생 (사람의 `Menutree및TestCase` 일체형) | OK |
| 12 | "완성" 기준 | 모든 상태·요소·흐름을 **종단까지 전수**. 개수 할당 없음 | OK |

---

## G. 구조 배치 규칙 (트리 문법) — 2026-06-24 PoC v3 QA 회신 분석

> 출처: `docs/feedback/PoC_FlowTree_doc82_v3_0624_김충효회신.xlsx` (76개 셀 코멘트 분석).
> A~F는 "어떤 노드를 만들지(타입·입도)"를 다뤘다면, G는 "노드를 트리 안에 어떻게 배치할지(문법)"를 다룬다.
> **doc82 사례에 한정된 수정이 아니라, 모든 기획서의 흐름 트리 작성에 적용되는 일반 규칙으로 일반화한 것.** 근거(행) 열은 어떤 코멘트에서 그 일반 규칙을 도출했는지 추적용일 뿐, 그 문서·그 케이스에만 적용되는 규칙이 아님.
> 상태: 초안 — 한 명의 QA 회신에서 도출한 패턴. 반복 검증 전까지는 FLOW_PROMPT에 우선 반영하고, 다음 PoC에서 재확인.

| # | 항목 | 규칙 | 근거(행) |
|---|------|------|---------|
| 13 | PR 위치 | PR은 동작(C/T/V 등) **직전에만** 위치. 동작과 동작 사이에 PR을 끼우지 않음 | 46 |
| 14 | PR 통합 | **하나의 시나리오를 게이트하는 조건이 2개 이상이면(트리의 다른 깊이에서 발견되더라도) 별도 PR 노드로 쪼개지 말고 그 시나리오의 루트 PR 하나의 content에 모아서** 작성. 단, 조건 값에 따라 **완전히 다른 결과로 갈리는 진짜 분기**는 깊은 곳에 중첩시키지 말고 그 값마다 별도의 루트 PR(별도 경로)로 분리 | 7(루트로 끌어올림), 36, 39 |
| 15 | **화면 전환 명시** | **상태 전이가 사용자 행동(클릭/선택/입력)으로 일어난 것이면 그 행동을 반드시 C/T로 명시하고, PR은 그 행동 이전 상태만 기술한다.** PR content에 이미 "~화면에 진입된 상태"처럼 결과를 담아버리고 행동 노드 없이 D가 바로 나오면 안 됨 — 화면/메뉴/팝업 전환은 예외 없이 사용자 행동이 개입하므로 PR→D 직결 금지. **사용자가 여러 필드 입력을 누적한 결과로 UI 상태가 바뀌는 경우(좁은 범위)는 PR→V(상태 확인)→D로 표현**(입력값 모두 채워짐 → V 확인버튼 확인 → D 버튼 활성화). **API 응답·시스템 자동 처리처럼 사용자 행동이 전혀 개입하지 않는 시스템 상태 분기는 PR→D 직결 허용**(범위는 0624 1차 적용 기준, 다음 PoC에서 재검증) | 4 (PR을 "미납조회 진입 상태"로 내리고 "미납대장 선택" C를 추가하라는 지적의 일반화), 59 (PR→V→D 예시) |
| 16 | 동작-결과 쌍 | 모든 동작(C/T/H/V 등)은 자식으로 최소 1개의 D/V 결과를 가져야 함(고아 동작 금지). 모든 D/V는 직전에 동작이 있어야 함(15항의 예외 제외, 고아 결과 금지) | 33,48,49,54,59,65,67,71,75 |
| 26 | **검증 분기 형제 유지** | **하나의 동작(예: 제출 버튼 클릭)에서 갈라지는 여러 검증 결과 분기(성공/실패 유형별)는 전부 그 동작의 직속 형제 자식으로 묶는다.** 분기 중 일부를 다른 동작 뒤·더 깊은 위치에 떼어놓으면 그 동작과의 연결이 끊겨 "동작없이 결과만 존재"처럼 보이게 됨 | 62(신청완료=성공), 65(정보불일치), 67(중복신청) — 셋 다 "신청하기 버튼 클릭"의 형제 분기여야 함 |
| 17 | 순차 결과 체이닝 | 한 동작의 결과가 **순차적으로 여러 개** 이어지면(분기 아님) 새 경로를 만들지 말고 **D→D로 같은 경로 안에서 계속 이어 붙임**. 새 경로(분기)는 실제로 다른 선택·다른 상태가 생길 때만 생성 | 4,5,6,35,37,38,40,41,44,45,51,52,55~57,61,63,64,66,68,69,72,73 (가장 빈번) |
| 18 | 단일 동작 원칙 | 한 노드에는 **단일 동작만** 기재. "체크박스 클릭 후 버튼 클릭"처럼 복합 행동을 하나로 합치지 말고 별도 노드(동작-결과 쌍)로 분리 | 28, 29, 62 |
| 19 | 메시지 텍스트 결합 | 한 메시지/팝업 안의 여러 줄 텍스트는 **한 D의 content에 `-`로 묶어서** 작성(줄마다 D 쪼개지 않음). 단, 장점1·2·3·4처럼 **서로 독립된 목록 항목은 그대로 항목마다 개별 D**(C·5·6항과 모순 아님 — "한 메시지의 줄바꿈"과 "독립 항목"을 구분) | 36 예시 |
| 20 | 공통 UI 요소 누락 금지 | 화면/팝업의 공통 요소(타이틀·단지명·닫기/확인 버튼·체크박스·플레이스홀더 등)는 **신규 기능과 무관해도 빠짐없이 D로 포함** | 22,24,25,26,27,43,54,55,56,57,60 |
| 21 | Summary 텍스트 제외 / 중복 노드 금지 | 기획서의 "게재 위치/대상/일정" 같은 **Summary·설명용 메타 텍스트**는 다른 케이스에 이미 반영돼 있으면 별도 D로 중복 생성하지 않음. **트리에 이미 존재하는 것과 같은 의미의 PR-결과 조합도 다른 위치에 또 만들지 않음**(중복 노드 금지로 일반화) | 11, 12, 13, 24(중복 PR/D 삭제 지적) |
| 22 | 페이지 순서 배치 | 트리 최상위 PR(루트)들은 **기획서에 등장하는 페이지 순서대로** 배치 | 4~10 |
| 23 | V vs C 구분 | V는 사용자 동작 없는 **시스템 자동 확인에만** 사용. 사용자가 실제로 클릭/탭하는 동작이면 V 대신 C로 표기 | 9, 23 |
| 24 | ~~상태 조건의 논리적 여집합 포함~~ → **여집합 자동 추가 금지로 정정 (2026-06-30 QA 회신)** | **[폐기]** 기존엔 "A인 경우"만 기획서에 있어도 "A가 아닌 경우"를 논리적 귀결로 보고 자동 보강하라고 했으나, **QA가 이 방침이 틀렸다고 피드백**. 두 경우(A인 경우/A가 아닌 경우)가 **기획서에 둘 다 명시됐을 때만** PR 분기로 분리하고, 한쪽만 있으면 **반대 경우를 추론해서 만들지 않는다** — 추론한 반대 결과("팝업이 안 보일 것" 등)가 실제로는 다를 수 있어 추측에 해당한다고 판단. (DEFAULT_FLOW_RULES 규칙13에 반영) | 31, 32 — 단, 이 행들에서 도출한 "그래도 있어야 한다"는 일반화는 QA 재검토 결과 폐기됨 |
| 25 | 입력 행위(T) 완전성 | 입력 필드는 D(필드 출력)만 만들고 끝내지 말고 **T(입력 행위) 노드도 빠짐없이** 만든다 — 필드마다 입력 행위 자체가 통째로 빠지는 경우가 잦았음. **T는 예외 없이 자식 D를 가져야 하며, 기획서에 별도 결과 문구가 없으면 사람 골든 TC의 표준 문구 "입력한 텍스트 반영"을 쓴다**(규칙6 원문 인용의 예외, 2026-06-25 QA가 골든 TC 캡처로 확인 — 동/호/신청자/휴대폰번호 입력 4건 모두 동일 패턴, FLOW_PROMPT T 정의에 반영) | 58 |
| 26 | **엑셀 행 분리(렌더링)** | 한 행 = 사전조건 PR(있으면) + 하나의 액션(C/DC/H/T) + 그 결과 D들(연속 캐스케이드). **D 뒤에 새 액션(C/DC/H/T 등)이 이어지면 액션이 바뀐 것이므로 새 행으로 분리**(V는 결과로 취급해 제외 — C/PR→V는 같은 행 유지). **이 규칙은 트리 생성 프롬프트가 아니라 `flow_tree_report.py`의 렌더러 코드(`_ROW_BREAKING_ACTIONS`)에 구현되어 있다** — Gemini가 만드는 건 JSON 트리 구조뿐이고, 그 트리를 몇 번째 엑셀 행/열에 펼칠지는 결정론적 코드가 전담한다 | 2026-06-30, v16 5행(D→H 한 행에 붙어있던 사례)에서 발견 |

---

## 합의 후 처리
- 확정된 내용은 [flow_tree_schema_design.md](flow_tree_schema_design.md) §4와 `tc_generator.py`의 `FLOW_PROMPT`에 반영한다.
- 수정 항목이 있으면 해당 규칙 문구만 교체 → 재추출로 즉시 반영 가능.
- 합의 완료 시 흐름 트리 정식 전환 구현(2단계 Excel·3단계 TC 선형화)에 착수.
- **G절은 별도 QA 확인 전 1차 반영**(FLOW_PROMPT에 적용됨). 다음 PoC 재추출 후 QA 재검토로 확정 필요.
- **2026-06-30 아키텍처 변경**: G절의 "노드 배치(트리 문법)" 규칙들은 `FLOW_PROMPT` 하드코딩에서 분리되어 **DB 룰셋(`QARuleSet.flow_rules`, 코드 기본값은 `DEFAULT_FLOW_RULES`)으로 이전**했다 — 웹 UI(룰셋 관리 화면)에서 코드 배포 없이 직접 수정 가능. 단, #26(엑셀 행 분리)처럼 "트리를 어떻게 렌더링할지"에 관한 규칙은 LLM이 따를 수 있는 종류가 아니므로 여전히 `flow_tree_report.py` 코드로만 구현된다.
